# secrets & runtime data
.env
.env.*
!.env.example
share.env
bot.config

# probe/test artifacts
*.xml
share_app.js
entry.js
core.js
_probe/

# build artifacts
build/
*.tar.gz

# browser-use skill (local third-party + dedicated env, not committed)
thrid-party/
.venv-browseruse/
.browser-use-profile/

# runtime data (persisted messages, spool, state)
data/
server_data/

# scheduler runtime artifacts (queue DB + WAL/SHM, single-instance lock, logs)
# data/ 已整体忽略;下列显式模式声明意图,并覆盖自定义路径下的同类产物。
*.db
*.db-wal
*.db-shm
*.lock
scheduler-health.json
logs/

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
